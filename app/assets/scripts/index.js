// grab btns
const getAddBtn = document.querySelector("#add-book");
const getLibBtn = document.querySelector("#library");

// array of book objects
let library = [];

// book objects

// filter books by searchText or if owned (boolean)
const filters = {
    searchText: "",
    owned: false
}

// event listeners

// adds book to collection when clicked
getAddBtn.addEventListener("click", () => {
});